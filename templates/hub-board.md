# Hub board — state that survives every chat

One line per item: `YYYY-MM-DD · project · task · state/next`. The hub (`commands/hub.md`) updates this on every dispatch and completion, and trims LANDED entries older than the window into a one-line dated digest in your conversation log index.

## ACTIVE

## WAITING ON USER

## LANDED (7 days)
